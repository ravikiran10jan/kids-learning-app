package com.kidslearning.api.model;

public enum ExerciseType {
    pick_image,
    pick_text,
    pick_number,
    spell_tiles,
    build_sentence,
    match_pairs,
    count_tap,
    number_line,
    sort_bins,
    true_false,
    listen_and_answer
}
